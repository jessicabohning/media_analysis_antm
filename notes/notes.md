# Project Notes

## UV
I created a venv through uv
* To activate uv call ```source .venv/bin/activate```
* To install a package through uv, call ```uv pip install <package_name>```
* To see uv installed packages call ```uv pip freeze```